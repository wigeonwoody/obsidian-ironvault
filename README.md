# Obsidian Iron Vault

This repository defines a baseline structure, configuration, and plugins
for derived Obsidian vaults.

This vault is based on `obsidian-baseline v1.0`

It is not a daily-use vault.

Changes to this repository are intentional, infrequent, and forward-looking.
They exist to improve future vaults, not to retrofit existing ones.

###### Setup your Copilot AI Model's API key
As with all vaults based on `obsidian-baseline`, you must:

Copy/rename `data.json.template` to `data.json` in the `.obsidian/plugins/copilot` directory and set your AI Model's API key in Copilot's plugin settings.


## Change Log
- 2025-01-03 (v1.0)
	- Readme update
	- Iron Vault
		- Character.template
		- Progress.template
		- Clock.template
	- Inline Scripts
		- Library
		- iron-vault.sfile
	- Homebrew